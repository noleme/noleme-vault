services:
    composite_service:
        class: com.noleme.vault.service.tag.CompositeService
        constructor:
            - '@composite_service_components'

    composite_service_components: tag
